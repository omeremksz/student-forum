import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main : "#fffff",
        },
        secondary: {
            main: "#f48024",
        },
        background: {
            default: "#f0f0f0",
        },
    }
})

export default theme;